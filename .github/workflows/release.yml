name: Build and Distribute Bun Executables

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Compile Bun Executable
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          # Linux
          - platform: bun-linux-x64
            extension: ''
          - platform: bun-linux-x64-baseline
            extension: ''
          - platform: bun-linux-x64-modern
            extension: ''
          - platform: bun-linux-arm64
            extension: ''
          # macOS
          - platform: bun-darwin-x64
            extension: ''
          - platform: bun-darwin-x64-baseline
            extension: ''
          - platform: bun-darwin-x64-modern
            extension: ''
          - platform: bun-darwin-arm64
            extension: ''
          # Windows
          - platform: bun-windows-x64
            extension: '.exe'
          - platform: bun-windows-x64-baseline
            extension: '.exe'
          - platform: bun-windows-x64-modern
            extension: '.exe'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Compile with Bun
        run: |
          bun build --compile --minify --bytecode ./index.ts --target=${{ matrix.platform }} --outfile pr-finder${{ matrix.extension }}

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: pr-finder-${{ matrix.platform }}
          path: pr-finder${{ matrix.extension }}
