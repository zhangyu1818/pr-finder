{
  "name": "pr-finder",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify --bytecode index.ts --outfile pr-finder"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@octokit/graphql": "^8.1.1",
    "commander": "^12.1.0"
  }
}